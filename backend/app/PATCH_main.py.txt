
# In app/main.py, add these two lines after the other route imports:
#
#   from app.api.routes import ml
#   app.include_router(ml.router)
#
# The full imports block should look like:
#
#   from app.api.routes import datasets, products, suppliers, analytics, ml
#   ...
#   app.include_router(ml.router)
